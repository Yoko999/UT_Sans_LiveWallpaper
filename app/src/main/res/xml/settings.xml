<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:title="@string/settings_title">

    <ListPreference
            android:key="preference_sleeping"
            android:title="@string/preference_sleep_title"
            android:summary="@string/preference_sleep_summary"
            android:entries="@array/bed_times"
            android:entryValues="@array/sleep_values" />

    <CheckBoxPreference android:key="preference_key_sound"
        android:title="@string/preference_sound"
        android:checked="true"/>

</PreferenceScreen>